name: Cleanup

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        description: "Name of the environment to destroy"
        options:
        - pr
        - dev

env:
  AWS_REGION : "ca-central-1"

permissions:
  id-token: write
  contents: read

jobs:
  Cleanup:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    defaults:
      run:
        working-directory: ./terraform
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_GITHUB_DEVOPS_ROLE }}
          role-session-name: GitHub_to_AWS_via_FederatedOIDC
          aws-region: ${{ env.AWS_REGION }}
          role-skip-session-tagging: true
          audience: sts.amazonaws.com

      - name: Terraform-Setup
        id: Setup
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: true

      - name: Terraform-Init
        id: Init
        run: terraform init
          -backend-config="dynamodb_table=main-tfstate-lock-1a6f390e" 
          -backend-config="key=${{ inputs.environment }}/ga-vm/terraform.tfstate"

      - name: Terraform-Destroy
        id: Destroy
        run: terraform destroy -var ENV=${{ inputs.environment }} -var ROLE_ARN=${{ secrets.ROLE_ARN }} -var EXTERNAL_ID=${{ secrets.EXTERNAL_ID }} -auto-approve
